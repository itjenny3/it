<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.tiny.dao.ArticleDao">
	<resultMap id="result" type="article">
		<result property="title" column="title" />
		<result property="content" column="content" />
	</resultMap>
	
	<insert id="save" parameterType="article">
		INSERT
		INTO `article` 
				(`title`,
				`content`)
		VALUES (#{title},
				#{content});
	</insert>
	
	<select id="get" parameterType="map" resultType="article">
		SELECT *
		FROM `article`
		WHERE `title` = #{title}
		LIMIT 1;
	</select>
	
	<delete id="delete" parameterType="map">
		DELETE
		FROM `article`
		WHERE `title` = #{title};
	</delete>
</mapper>